{
  "name": "datt-exam-proctor-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs scripts/*.test.mjs",
    "verify": "npm run test && npm run build"
  }
}
