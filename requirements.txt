# MediaPipe and Ultralytics declare the contrib and standard OpenCV wheels
# respectively. Pin both to the same release so their shared cv2 files cannot
# come from different versions. OpenCV 4.12+ requires NumPy 2 on Python >=3.9,
# while this MediaPipe/FaceNet stack is validated with NumPy 1.26.
opencv-python>=4.9,<4.12
opencv-contrib-python>=4.9,<4.12
mediapipe==0.10.14
jax<0.5
jaxlib<0.5
facenet-pytorch>=2.5,<3
Pillow>=10.2,<10.3
qrcode[pil]>=7.4,<9
ultralytics>=8.2,<9
numpy>=1.26,<2
pyyaml>=6.0,<7
pydantic>=2.7,<3
pytest>=8.0,<9
jinja2>=3.1,<4
matplotlib>=3.9,<4
xhtml2pdf>=0.2.17,<0.3

# requirements.txt chay du ca client CV va backend. Container/backend-only co
# the dung requirements-backend.txt nhe hon nho cac package chung lazy-import.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9,<3
python-jose[cryptography]>=3.3,<4
passlib[bcrypt]>=1.7,<2
# Ghim duoi 4.1: bcrypt>=4.1 sang bao ValueError ("password cannot be longer
# than 72 bytes") ngay trong buoc tu kiem tra noi bo cua passlib
# (detect_wrap_bug goi 1 chuoi test dai >72 byte) - passlib 1.7.4 (ban moi
# nhat hien co) chua tuong thich voi thay doi nay cua bcrypt. Xac nhan bang
# cach chay that: bcrypt 5.0.0 lam moi test backend crash, ghim 4.0.1 het loi.
bcrypt<4.1
python-multipart>=0.0.9,<1
httpx>=0.27,<1
websockets>=12,<17
google-auth[requests]>=2.40,<3
